{
  "name": "website",
  "version": "0.1.0",
  "scripts": {
    "start": "cd daServer && npm start",
    "heroku-postbuild": "npm install && npm build",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
